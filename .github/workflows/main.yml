name: Portfolio CI Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Show project files
        run: ls

      - name: Build message
        run: echo "Portfolio project build successful"
